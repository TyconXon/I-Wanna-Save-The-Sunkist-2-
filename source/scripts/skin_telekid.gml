if (argument0=="mask") {
    script_execute(skin_default,"mask")
}

if (argument0=="step") {
    script_execute(skin_default,"step")
}

if (argument0=="draw") {
    script_execute(skin_default,"draw")
}
